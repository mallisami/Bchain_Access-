For OFFLINE PREVIEW ONLY. This IEEEtran.cls is a lightweight fallback (built on
article.cls) so the paper compiles where the official IEEEtran is unavailable.
Do NOT use it for submission. On Overleaf / TeX Live, the real IEEEtran is used
automatically by \documentclass[journal,onecolumn]{IEEEtran}.
